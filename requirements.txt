python-telegram-bot[job-queue]>=20.0
flask>=3.0
python-dotenv>=1.0
pytest>=8.0
httpx>=0.27
# Runtime dependencies of rapidocr-onnxruntime, spelled out so that package can be
# installed with --no-deps (requirements-ocr.txt): its own metadata asks for the
# full opencv-python, whose cv2.so links libGL and cannot import on a headless host.
onnxruntime>=1.20
opencv-python-headless>=4.9
numpy>=1.26
Pillow>=10.0
pyclipper>=1.3
PyYAML>=6.0
Shapely>=2.0
six>=1.16
